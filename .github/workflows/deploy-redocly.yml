name: Deploy to Redocly

on:
  push:
    branches: [main]
    paths:
      - 'openapi/salesos-api-v3.0.yaml'
      - 'openapi/salesos-api-v2.1.yaml'
      - 'redocly.yaml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Redocly CLI
        run: npm install -g @redocly/cli@latest

      - name: Deploy to Redocly
        env:
          REDOCLY_AUTHORIZATION: ${{ secrets.PLAY2SELL_REDOCLY }}
        run: |
          echo "🚀 Deploying SalesOS API v3.0 to Redocly..."
          redocly push openapi/salesos-api-v3.0.yaml \
            --organization play2sell \
            --project salesos-api \
            --branch main \
            --mount-path /v3.0 \
            --author "GitHub Actions <actions@github.com>" \
            --message "Auto-deploy from GitHub Actions"

          echo "🚀 Deploying SalesOS API v2.1 to Redocly..."
          redocly push openapi/salesos-api-v2.1.yaml \
            --organization play2sell \
            --project salesos-api \
            --branch main \
            --mount-path /v2.1 \
            --author "GitHub Actions <actions@github.com>" \
            --message "Auto-deploy from GitHub Actions"

      - name: Success notification
        run: |
          echo "✅ APIs deployed successfully to Redocly!"
          echo "📖 View documentation at:"
          echo "   - v3.0: https://redocly.com/docs/salesos-api/v3"
          echo "   - v2.1: https://redocly.com/docs/salesos-api/v2.1"
